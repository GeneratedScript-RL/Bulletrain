--!strict

local SharedComponents = {
	FrameComponents = require(script.FrameComponents),
	FrameDictionary = require(script.FrameDictionary),
	ObjectComponents = require(script.ObjectComponents),
}

return SharedComponents
